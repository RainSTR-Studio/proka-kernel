[package]
name = "proka-fs"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.58", features = ["derive"] }
colored = "3.1.1"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
