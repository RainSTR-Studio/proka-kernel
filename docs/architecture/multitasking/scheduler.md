# 进程间通信 (IPC)

IPC 机制允许不同的任务之间交换数据和同步状态。

## 规划模型

- **管道 (Pipes)**：通过内存缓冲区实现字节流通信。
- **信号 (Signals)**：轻量级的异步通知机制。
- **共享内存**：允许进程直接访问同一物理页，实现零拷贝数据传输。

## 待完善内容

- [ ] 基于 VFS 的匿名管道实现。
- [ ] 系统调用层面的 IPC 封装。
